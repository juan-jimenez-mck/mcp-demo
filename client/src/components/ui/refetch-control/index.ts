export { default as RefetchControl } from './components/refetch-control';
export type { RefetchInterval, IntervalOption } from './types';
export { RefetchIntervals, RefetchIntervalOptions } from './constants';
